// def two_oldest_ages(ages):
//     sorted_ages = sorted(ages)
//     return [sorted_ages[-2], sorted_ages[-1]]



// def sum_of_minimums(numbers):
//     return sum(min(row) for row in numbers)


//def uefa_euro_2016(teams, scores):
    // team1, team2 = teams
    // score1, score2 = scores

    // if score1 > score2:
    //     result = f"{team1} won!"
    // elif score1 < score2:
    //     result = f"{team2} won!"
    // else:
    //     result = "teams played draw."

    // return f"At match {team1} - {team2}, {result}"




  //  29.07.2025 homework

//bu function da array 1 bilan array 2 sort qilib qo'shilyapti 
//   def merge_arrays(arr1, arr2):
//     return sorted(set(arr1 + arr2))


// bu functionda array ichidagi qiymatlar array methods yordamida o'ng dan chapga, yoki chapdan o'ng ga ko'chirilyapti 
//function loopArr(arr, direction, steps) {
    // for (let i = 0; i < steps; i++) {
    //     if (direction === 'left') {
    //       arr.push(arr.shift());
    //     } else if (direction === 'right') {
    //       arr.unshift(arr.pop());
    //     }
    //   }
    //   return arr;
    // }


// bu function da 2 ta array ichida harflar va raqamlar bor, arraylarni birlashtirib raqamlar bir biriga qo'shilyapti
    // function combine(...objects) {
    //     return objects.reduce((acc, obj) => {
    //       for (let key in obj) {
    //         acc[key] = (acc[key] || 0) + obj[key];
    //       }
    //       return acc;
    //     }, {});
    //   }
      


//bu function da includes yordamida array ichida birorta qiymat bormi yoki yoq tekshirilyapti
    // function check(a, x) {
    //     return a.includes(x);
    //   }
      


//bu functionda math.min/max yordamida raqamlardan eng kattasi va eng kichigi aniqlanyapti
    // function minMax(arr) {
    //     return [Math.min(...arr), Math.max(...arr)];
    //   }
      
    